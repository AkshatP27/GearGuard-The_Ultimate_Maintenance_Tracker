# GearGuard - Project Architecture

## 📁 Complete File Structure

```
GearGuard/
│
├── 📂 frontend/                          # React Frontend Application
│   ├── 📂 src/
│   │   ├── 📂 components/
│   │   │   ├── 📂 auth/
│   │   │   │   ├── LoginForm.jsx        # Enhanced login with demo
│   │   │   │   ├── SignUpForm.jsx       # Registration form
│   │   │   │   └── ProtectedRoute.jsx   # Route protection HOC
│   │   │   └── 📂 common/
│   │   │       ├── Navbar.jsx           # Navigation bar
│   │   │       └── Sidebar.jsx          # Side navigation
│   │   │
│   │   ├── 📂 context/
│   │   │   └── AuthContext.jsx          # Auth state management + demo support
│   │   │
│   │   ├── 📂 pages/
│   │   │   ├── AuthPage.jsx             # ⭐ NEW: Tabbed auth page
│   │   │   ├── LoginPage.jsx            # Login page wrapper
│   │   │   ├── SignUpPage.jsx           # ⭐ NEW: Signup page wrapper
│   │   │   ├── DashboardPage.jsx        # Main dashboard
│   │   │   ├── EquipmentPage.jsx        # Equipment management
│   │   │   ├── MaintenancePage.jsx      # Maintenance tracking
│   │   │   └── TeamsPage.jsx            # Team management
│   │   │
│   │   ├── 📂 services/
│   │   │   └── supabase.js              # Supabase client
│   │   │
│   │   ├── 📂 styles/
│   │   │   └── index.css                # Global styles
│   │   │
│   │   ├── App.jsx                      # ⭐ UPDATED: Main app with routing
│   │   └── main.jsx                     # Entry point
│   │
│   ├── index.html                       # HTML template
│   ├── package.json                     # Frontend dependencies
│   ├── vite.config.js                   # Vite configuration
│   ├── tailwind.config.js               # TailwindCSS config
│   ├── postcss.config.js                # PostCSS config
│   ├── .env.example                     # Environment template
│   └── .gitignore                       # Git ignore rules
│
├── 📂 backend/                           # ⭐ NEW: Express.js Backend API
│   ├── 📂 src/
│   │   ├── 📂 config/
│   │   │   └── supabase.js              # Supabase server config
│   │   │
│   │   ├── 📂 controllers/
│   │   │   └── authController.js        # Auth logic + demo support
│   │   │
│   │   ├── 📂 middleware/
│   │   │   └── auth.js                  # JWT authentication
│   │   │
│   │   ├── 📂 routes/
│   │   │   └── authRoutes.js            # Auth API routes
│   │   │
│   │   ├── 📂 models/                   # (Ready for future models)
│   │   │
│   │   └── server.js                    # Express server setup
│   │
│   ├── package.json                     # Backend dependencies
│   ├── .env.example                     # Backend env template
│   ├── .gitignore                       # Git ignore rules
│   └── README.md                        # Backend documentation
│
├── README.md                            # Main project README
├── QUICK_START.md                       # ⭐ NEW: Quick start guide
├── IMPLEMENTATION_SUMMARY.md            # ⭐ NEW: Detailed summary
└── .gitignore                           # Root git ignore

```

## 🔄 Application Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      User Opens App                         │
│                    http://localhost:5173                     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   App.jsx (Router)                          │
│              Wrapped with AuthProvider                       │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              Check Authentication State                      │
│                  (AuthContext)                              │
└────────┬────────────────────────┬───────────────────────────┘
         │                        │
    Not Authenticated        Authenticated
         │                        │
         ▼                        ▼
┌────────────────────┐   ┌───────────────────────┐
│   Redirect to      │   │  Allow Access to      │
│   /auth Page       │   │  Protected Routes     │
└────────┬───────────┘   └───────┬───────────────┘
         │                        │
         ▼                        ▼
┌────────────────────┐   ┌───────────────────────┐
│   AuthPage.jsx     │   │    Dashboard          │
│  ┌──────────────┐  │   │    Equipment          │
│  │ Login Tab    │  │   │    Maintenance        │
│  │ SignUp Tab   │  │   │    Teams              │
│  │ Demo Button  │  │   └───────────────────────┘
│  └──────────────┘  │
└────────┬───────────┘
         │
         ▼
┌────────────────────────────────────────┐
│     Submit Credentials                 │
│  (demo@gearguard.com / demo123)        │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│        AuthContext.signIn()            │
│    Checks if demo credentials          │
│    OR calls Supabase auth              │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│    Set User State + localStorage       │
│    Redirect to /dashboard              │
└────────────────────────────────────────┘
```

## 🔐 Authentication Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              AuthContext Provider                  │    │
│  │                                                     │    │
│  │  State:                                            │    │
│  │  • user: User object or null                       │    │
│  │  • loading: Boolean                                │    │
│  │                                                     │    │
│  │  Methods:                                          │    │
│  │  • signIn(email, password)                         │    │
│  │  • signUp(email, password, metadata)               │    │
│  │  • signOut()                                       │    │
│  │                                                     │    │
│  │  Demo Logic:                                       │    │
│  │  • Checks for demo credentials                     │    │
│  │  • Stores demo user in localStorage                │    │
│  │  • Bypasses Supabase for demo                      │    │
│  └─────────────────────┬──────────────────────────────┘    │
│                        │                                    │
│                        ▼                                    │
│  ┌────────────────────────────────────────────────────┐    │
│  │            Protected Routes                        │    │
│  │  • Checks user state                               │    │
│  │  • Shows loading spinner                           │    │
│  │  • Redirects if not authenticated                  │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   │ HTTP Requests
                   │ (For real auth)
                   ▼
┌──────────────────────────────────────────────────────────────┐
│                        Backend API                           │
│                   http://localhost:5000                      │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │            Auth Controller                         │    │
│  │                                                     │    │
│  │  POST /api/auth/login                              │    │
│  │  • Checks for demo credentials                     │    │
│  │  • Returns demo user OR                            │    │
│  │  • Calls Supabase auth                             │    │
│  │                                                     │    │
│  │  POST /api/auth/signup                             │    │
│  │  • Creates user via Supabase                       │    │
│  │  • Returns user + session                          │    │
│  │                                                     │    │
│  │  POST /api/auth/logout                             │    │
│  │  • Clears Supabase session                         │    │
│  │                                                     │    │
│  │  GET /api/auth/profile (protected)                 │    │
│  │  • Requires JWT token                              │    │
│  │  • Returns user profile                            │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   │ Supabase Client
                   ▼
┌──────────────────────────────────────────────────────────────┐
│                      Supabase                                │
│                                                              │
│  • Authentication Service                                    │
│  • User Management                                           │
│  • PostgreSQL Database                                       │
│  • Real-time Subscriptions                                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 🎯 Key Features Implementation

### 1. Demo Login System
```
Frontend: AuthContext checks credentials
         ↓
If demo credentials match:
  ├─ Create demo user object
  ├─ Store in localStorage
  └─ Set user state
         ↓
Else:
  └─ Call Supabase auth
```

### 2. Protected Routes
```
User navigates to protected route
         ↓
ProtectedRoute component checks:
  ├─ Is loading? → Show spinner
  ├─ No user? → Redirect to /auth
  └─ Has user? → Render children
```

### 3. Form Validation
```
SignUp Form:
  ├─ Email format validation
  ├─ Password length check (min 6)
  ├─ Password match confirmation
  └─ Required field checks
```

## 📊 Route Protection Matrix

| Route         | Protected | Redirect To | Notes                    |
|---------------|-----------|-------------|--------------------------|
| /             | No        | /auth       | Root redirect            |
| /auth         | No        | -           | Public auth page         |
| /login        | No        | -           | Public login page        |
| /signup       | No        | -           | Public signup page       |
| /dashboard    | Yes       | /auth       | Requires authentication  |
| /equipment    | Yes       | /auth       | Requires authentication  |
| /maintenance  | Yes       | /auth       | Requires authentication  |
| /teams        | Yes       | /auth       | Requires authentication  |

## 🔧 Technology Stack Diagram

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend Stack                     │
├─────────────────────────────────────────────────────────┤
│  React 18          │  Modern UI library                 │
│  Vite              │  Fast build tool & dev server      │
│  React Router v6   │  Client-side routing               │
│  TailwindCSS       │  Utility-first CSS                 │
│  Supabase Client   │  Auth & database client            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                      Backend Stack                      │
├─────────────────────────────────────────────────────────┤
│  Express.js        │  Web application framework         │
│  Supabase          │  Authentication & database         │
│  JWT               │  Token-based authentication        │
│  CORS              │  Cross-origin resource sharing     │
│  dotenv            │  Environment variable management   │
└─────────────────────────────────────────────────────────┘
```

---

**Legend:**
- ⭐ NEW: Newly created files/features
- 📂 Folder
- 📄 File
- ✅ Completed
- 🔄 In Progress
